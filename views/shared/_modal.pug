mixin modal
    #project-modal.modal
        .preloader-wrapper.small.active
            .spinner-layer
                .circle-clipper.left
                    .circle
                .gap-patch
                    .circle
                .circle-clipper.right
                    .circle